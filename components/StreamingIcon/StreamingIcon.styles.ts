import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  icon: {
    borderRadius: 10,
    height: 50,
    width: 50,
    // borderWidth: 2,
    // borderColor: 'white',
    // borderStyle: 'solid',
  },
});
