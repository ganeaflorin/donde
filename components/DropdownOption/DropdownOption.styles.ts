import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  optionContainer: {
    display: 'flex',
    flexDirection: 'row',
  },
  poster: {
    height: 70,
    width: 50,
  },
  posterContainer: {
    margin: 10,
  },
  textContainer: {
    marginTop: 10,
  },
});
